import {ThousandSpacePipe} from './thousand-space.pipe';

describe('ThousandSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new ThousandSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
